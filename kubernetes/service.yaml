apiVersion: v1
kind: Service
metadata:
  name: cove-user-backend-service
  namespace: ${NAMESPACE}
spec:
  selector:
    app: cove-user-backend
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8090  # The port your Spring Boot app is listening on (usually 8080)